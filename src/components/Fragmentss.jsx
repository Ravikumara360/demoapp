const Fragmentss=() =>
{
    return (
        <>
        <h1>This is custom components</h1>
        <p>Lorem</p>
        </>
    );
}
export default Fragmentss;

/*
Fragments 
 In react components, if we need to return multiplemJSX we have to enclose it in one outer container.

 To prevent the multiple duplicate divisions, we can use JSX fragments <> </>
 */